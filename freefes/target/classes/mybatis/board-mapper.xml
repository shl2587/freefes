<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
	"-//mybatis.org//DTD Mapper 3.0//EN" 
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ohmija.repository.BoardDAO">

	<select id="select_month" resultType="board" parameterType="string">
		select * from board
			where to_char(start_date, 'yyyy-MM') = #{month}
				or to_char(end_date, 'yyyy-MM') = #{month}
            order by idx
	</select>
	
	<select id="select_coming" resultType="board">
		select * from board
			where end_date &gt;= sysdate
			order by start_date
			fetch first 10 rows only
	</select>
	
	<select id="select_top10" resultType="board">
		select rownum as rank, idx, start_date, end_date, title, favorites, grade, region, file_name, file_path, festival_category
			from board
				where end_date &gt;= sysdate
				order by favorites desc
				fetch first 10 rows only
	</select>
	
	
</mapper>